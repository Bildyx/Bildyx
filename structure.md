# Bildyx

## Overview
Job/career reference platform (industries, countries, cities, jobs, organizations, skills, etc.) with a marketing website and a backend API.

## Tech Stack
- **Language**: TypeScript (backend), PHP/HTML (website)
- **Framework**: Express (API), Prisma (schema/migrations only)
- **Key dependencies**: Kysely (actual query builder, using `apps/api/src/db/types.ts` generated by kysely-codegen), Zod (API validation), pg
- **Build / runtime**: npm workspaces (root `package.json`), tsx

## Project Structure
```
apps/
├── api/                  # Backend API (Express + Kysely + Prisma schema)
│   ├── prisma/
│   │   └── schema.prisma # Source of truth for DB schema (Prisma migrations)
│   ├── src/
│   │   ├── db/types.ts   # Kysely types generated from live DB (kysely-codegen) — can drift from schema.prisma if migrations aren't regenerated
│   │   ├── models/        # Zod schemas per resource, field names match snake_case DB columns
│   │   └── routes/        # Express routes per resource
│   ├── scripts/
│   │   ├── generate_excel_templates.py  # Generates .xlsx import templates from schema.prisma; also locks them down (see below)
│   │   └── excel_to_csv.py              # Converts apps/api/data/excel_templates/*.xlsx into apps/api/data/*.csv (skips fully blank rows)
│   └── data/
│       ├── *.csv                 # CSVs generated from data/excel_templates via excel_to_csv.py
│       └── excel_templates/      # Generated .xlsx templates (one per reference model) — also doubles as the filled-in working data file (excel_to_csv.py reads from here, not a separate data/excel/)
└── website/               # Marketing site (PHP/HTML/CSS/JS)
Files/                      # Legacy hand-curated business data templates (csv/excel), unrelated to DB schema column names
```

## Key Files
| File | Role |
|------|------|
| `apps/api/prisma/schema.prisma` | Prisma schema — canonical model/field definitions |
| `apps/api/src/db/types.ts` | Kysely DB types (generated, may be stale vs. schema.prisma) |
| `apps/api/src/database.ts` | Kysely instance setup (Postgres/PGlite) |
| `apps/api/scripts/generate_excel_templates.py` | Parses schema.prisma and emits one Excel template per reference-data model |

## Architecture Notes
- The API layer uses **Kysely**, not Prisma Client, for queries. Prisma is used only for schema/migrations. Field names used in code (`models/*.ts`, `db/types.ts`) are DB column names (snake_case via `@map`/`@@map`), not the Prisma camelCase model field names.
- `Country.officialLanguages`, `Organization.numberOfEmployees`, and `Organization.numberOfSubsidiaries` intentionally have no `@map` in `schema.prisma` — confirmed via the applied migration SQL (`apps/api/prisma/migrations/20260708013135_init/migration.sql`) that the real DB columns are camelCase (quoted), matching `schema.prisma` and `db/types.ts` as-is. `organizations.csv`'s header uses the same camelCase for these two columns; the seed script reads them accordingly. The snake_case headers for these fields in `apps/api/data/*_rows.csv` are from a manually reformatted export, not the real column names — don't use those CSVs as a naming reference.
- Every FK field that logically points at an auto-generated UUID (`City.id`, `Organization.id`) must be resolved from the CSV's human-readable name via a DB lookup in the seed script (see `buildNameLookup` in `apps/api/prisma/seed-utils.ts`, used by `seeds_universities.ts`/`seeds_certifications.ts`/`seeds_subjects.ts`/`seeds_military_capabilities.ts`) — the CSVs can't contain the real UUID since it doesn't exist until insertion. This does **not** apply to `Country`, since `Country.isoCode` is a stable natural key (2-letter code) known ahead of time; `City.countryId`/`University.countryId`/etc. reference it directly as `@db.Char(2)`, no lookup needed.
- Models considered "reference data" (not tied to site auth/job-ad workflow): Industry, Country, City, Job, Organization, Skill, Certification, University, StudyFields, MilitaryCapabilities, Degree, Subject. These are the ones covered by `generate_excel_templates.py`. Excluded: JobAd, JobAdSkill, User, UserSession, UserProfile, UserCertifications, UserEducations, UserEducationFields, AuditLog.

## Current Status
Excel import template generator added and working for all 11 target reference models. All 11 templates that still match their current schema model (all except `military_capabilities.xlsx`, see below) are now locked down against import errors.

## Recent Changes
- [2026-07-14] Fixed `src/tests/schema.sql` (hand-maintained PGlite bootstrap fixture used by every test file, not auto-generated from `schema.prisma`): it had 4 tables whose later `CREATE INDEX`/`ALTER TABLE` statements referenced the Prisma *model* name (e.g. `"UserCertifications"`, `"UserEducations"`, `"UserEducationFields"`, `"Subject"`) instead of the real `@@map`'d table the `CREATE TABLE` actually used (`user_certifications`, `user_educations`, `user_education_fields`, `subjects`) — Postgres is case-sensitive on quoted identifiers, so every test run failed at bootstrap with `relation "X" does not exist` before a single test could execute, for the entire suite. Confirmed via `git show 3476670:...schema.sql` that this predates all of today's schema work (not a regression from anything above). Fixed all mismatched references (left the merely-cosmetic constraint/index *names* alone, e.g. `"UserEducations_pkey"`, since Postgres doesn't require those to match a table name) and re-verified with a script cross-checking every `CREATE TABLE` against every later table reference (zero mismatches now). Full suite went from 0 tests able to run at all to 300 passing / 296 failing — the remaining failures are pre-existing, unrelated to this fix or to today's relation/seed audit (stale test fixtures using columns the schema no longer has: `countries.id` (real PK is `iso_code`), `skills.categories` (real field is singular `category`), `industries.color` (field doesn't exist) — same root cause as the 45 pre-existing `tsc --noEmit` errors in `src/routes|tests`, not touched here).
- [2026-07-14] Verification pass on the fixes below found the `MilitaryCapabilities` restoration had already been intentionally reverted (see the entry below this one) — re-confirmed via a user prompt that leaving `schema.prisma` without the model (while `src/routes/military_capabilities.ts`/`src/models/military_capabilities.ts`/`src/db/types.ts` keep working against the DB table via Kysely, independent of Prisma) is the correct, deliberate state. `npx tsx --test src/tests/military_capabilities.test.ts` passes (13/13 sub-tests) against the `schema.sql`-fixed PGlite DB, confirming the Kysely-only path is fully functional without a Prisma model. No further action needed on this point.
- [2026-07-14] `generate_excel_templates.py`: sheet protection was blocking column/row resizing by Excel's default, so a cell with long text made the next column unreadable without clicking into it, with no way to widen the previous column. Resizing is purely cosmetic (doesn't allow writing outside authorized cells or touching the data validation), so `formatColumns`/`formatRows` are now explicitly allowed (factored into a small `enable_sheet_protection()` helper) while everything else stays locked.
- [2026-07-14] Fixed the seed pipeline being fully broken past `cities` (per a full relation audit of `schema.prisma`): (1) restored the `MilitaryCapabilities` model (exact pre-removal shape, see git commit `66deef6` which had deleted it along with `Organization.military_capabilities`) — `seed.ts`/`seeds_military_capabilities.ts` were still calling `prisma.militaryCapabilities.createMany(...)` against a schema with no such model, so `npm run seed` would crash there; the already-applied baseline migration (`20260708061904_init`) still has the `military_capabilities` table, so no new migration is needed for this. (2) Rewrote `seeds_organizations.ts`: it and `organizations.csv` both still used the *pre-`66deef6`* `Organization` shape (`legal_status`, `activities`, `partnerships`, `founder`, `equipments`, single `type` column — see the "Found but not fixed" note below) while the current schema has `subtype`/`type1`/`type2`, `programsActivities`, `partners`, `founders`, `facilities`, no `legalStatus`; the old seed also passed `knownFor` as a `string[]` when the field is a plain `String?`. Seed now maps CSV columns to their current schema equivalents (`type`→`subtype` enum, `activities`→`programsActivities`, `partnerships`→`partners`, `founder`→`founders` (wrapped in an array), `equipments`→`facilities`); `legal_status` has no home in the current schema and is dropped; `authority`/`jurisdiction`/`members`/`collections`/`graduates`/`undergraduates`/`personnel`/`type1`/`type2` have no source column in `organizations.csv` and stay `null`. `seedOrganizations` runs before jobs/certifications/universities/subjects/militaryCapabilities in `seed.ts` and all of those depend on organizations existing, so this alone was blocking effectively the entire seed beyond `industries`/`countries`/`cities`. Also fixed smaller `schema.prisma` inconsistencies found in the same audit: added `@@index([city_id])` on `Organization` (FK had no index, unlike its sibling `parentOrganizationId`); renamed the Prisma-auto-generated relation name `organizations_city_idTocities` to `OrganizationHeadquarters` (Prisma-level only, no DB impact); renamed several `to-one` relation fields that were incorrectly named in the plural (`User.organizations`→`organization`, `Subject.organizations`→`organization`, `UserCertifications.certifications`/`.user_profiles`→ singular, `UserEducations.degrees`/`.universities`/`.user_profiles`→ singular, `UserEducationFields.StudyFields`→`studyField` (was colliding with the model's own name), `AuditLog.users`→`user`, `Skill.JobAdSkills`→`jobAdSkills`) — none of these affect the DB or `src/db/types.ts` since the API queries via Kysely, not Prisma Client (see Architecture Notes); added `@updatedAt` to `Subject.updated_at`/`StudyFields.updated_at`/`MilitaryCapabilities.updated_at` (previously no default at all, would reject any Prisma Client insert that didn't supply it explicitly — current seeds already did, so no behavior change there); removed a redundant `@map("currency")` on `City.currency` (name already matched). **Verified**: `npx prisma validate`, `npx prisma generate`, and `npx tsc --noEmit` all pass with zero new errors (pre-existing 45 errors in `src/routes|tests` are unrelated stale-`db/types.ts`-vs-Kysely-query drift, not touched by this change). **Not done** (needs a real migration + live DB, deliberately not run here): `StudyFields` still has no `@@map` — its physical table is `"StudyFields"` (PascalCase) instead of `study_fields` like every other reference table, which also means `src/db/types.ts` and `src/tests/user_education_fields.test.ts` hardcode `"StudyFields"` and would need updating in the same pass as the migration. The `@@index([city_id])` addition also needs `prisma migrate dev`/`deploy` run against the real DB to actually take effect. Neither `organizations.xlsx`/`.csv` nor `military_capabilities.xlsx` were regenerated from the current schema (still tracked in the note below) — only the seed script's *mapping* was fixed to make what already exists in `organizations.csv` insertable again.
- [2026-07-14] Reverted the `MilitaryCapabilities` restoration above (product decision: the model's removal in `66deef6` was intentional, not accidental) — removed the `MilitaryCapabilities` model and `Organization.militaryCapabilities` from `schema.prisma` again, deleted `seeds/seeds_military_capabilities.ts`, and dropped its import/call from `seed.ts`. Deliberately left `src/routes/military_capabilities.ts`, `src/models/military_capabilities.ts`, `src/tests/military_capabilities.test.ts`, and `src/db/types.ts`'s `MilitaryCapabilities`/`military_capabilities` entries untouched — the route/model layer queries via Kysely (`src/db/types.ts`, generated from the live DB) rather than Prisma Client, so it doesn't depend on `schema.prisma` and keeps working since the underlying `military_capabilities` table still exists in the DB (no migration was run). Only `seeds_military_capabilities.ts` used Prisma Client (`prisma.militaryCapabilities.createMany`) and needed removing alongside the schema model.
- [2026-07-14] Locked down `apps/api/data/excel_templates/*.xlsx` against import errors, with zero data loss (verified cell-by-cell against the pre-change files). `generate_excel_templates.py`: added a `--protect-existing` mode that adds protection/validation to already-filled templates in place (data untouched) — reruns safely (clears old validations first instead of stacking duplicates). Every column now gets a type-matching Excel data validation with an English error message (`whole`/`decimal`/`date`/`TRUE,FALSE` list, or an enum dropdown), the header row and any column outside the defined fields are locked via `ws.protection.sheet`, and `wb.security.lockStructure` blocks adding/renaming/deleting sheets. **Only the set of columns is restricted, not the number of rows** — some of these are filled to 100k+ rows, so unlocking/validation is applied at the Excel column level (`ws.column_dimensions[col].protection`/data-validation `sqref` up to row 1,048,576), which Excel applies as that column's default style for every row without creating a per-row cell object, rather than looping over a capped row range. Existing filled rows in `--protect-existing` mode are additionally unlocked cell-by-cell (bounded by the file's real current size, so still cheap) since already-styled cells override the column default. No password was set on either protection (guardrail against accidental input errors, not a security barrier — team can unprotect via Excel's UI if a template needs a real structural edit). Enum dropdowns are now sourced from a `veryHidden` `_lists` helper sheet (range reference) instead of an inline comma string, which also fixes a pre-existing bug where large enums (e.g. `Currency`, ~154 values) silently lost their dropdown on openpyxl resave (Excel's inline-list validation has a 255-character cap; a range reference doesn't). `excel_to_csv.py` now skips fully-blank rows (defensive, in case of stray touched-but-empty rows).
- **Found but not fixed (out of scope for the locking work above):** `apps/api/data/excel_templates/organizations.xlsx` currently has 0 data rows and a header that no longer matches `schema.prisma`'s `Organization` model (missing `type1`, `type2`, `programs_activities`, `partners`, `founders`, `facilities`, `authority`, `jurisdiction`, `members`, `collections`, `graduates`, `undergraduates`) — those columns exist in the current schema but not in the xlsx, so `--protect-existing` skipped them (only the matching columns got locked/validated). Meanwhile `apps/api/data/organizations.csv` (already in the repo) still holds real data under the *old* header shape — re-running `excel_to_csv.py` on the current (empty) `organizations.xlsx` would silently overwrite that CSV with an empty one. Don't run `excel_to_csv.py` for organizations until the xlsx template is regenerated/refilled to match the current schema. Similarly `skills.xlsx` is missing a `category` column present in the current `Skill` model (that one column wasn't locked/validated; the rest of the file was). Separately, `MilitaryCapabilities` no longer exists in `schema.prisma` (schema now has no model by that name) even though `apps/api/data/excel_templates/military_capabilities.xlsx` still exists on disk with data — it was left completely untouched since there's no schema model to derive its column types from.
- [2026-07-08] Fixed the full schema→seed→CSV pipeline ahead of the first real data load (audit found it wasn't insertable at all). Schema (`schema.prisma`): `User.role`'s `@default(USER)` referenced a value removed from the trimmed `UserRole` enum (invalidated `prisma generate` entirely) → `@default(CANDIDATE)`; `City/University/JobAd/UserProfile.countryId` are now `@db.Char(2)` to match `Country.isoCode` (verified via `prisma migrate diff` that a UUID/untyped FK column pointed at a `char(2)` unique column produces DDL Postgres would reject); `City.currency` (was capitalized `Currency`, inconsistent with the rest of the file) renamed to lowercase. Seeds (`apps/api/prisma/seeds/*.ts`): `seeds_cities.ts` never mapped the (required) `currency` field, had `largestOrganizations` instead of the real `largest_organization` field, passed `Language` instead of `Language[]` to `language`, and fed raw `number_of_airports` text (~100/101 rows have parenthetical airport names, e.g. `"1 (via Cairo Intl. Airport)"`) into an `Int` field — added `toIntLoose()` in `seed-utils.ts` to extract the leading digits; also now skips (with a `console.warn`) the 2 cities whose `country_id` (`AE`, `UZ`) has no matching row in `countries.csv`. `seeds_countries.ts`: `isoCode` (primary key) had no guard against a blank CSV cell, `currency` was assigned as a raw string instead of parsed against the `Currency` enum, and `ethnicGroups`/`religion` (plain `String?` fields) were wrongly run through `toStringArray`. `seeds_skills.ts` imported a `SkillCategory` enum that doesn't exist in the schema; `skills.csv`'s `categories` column now flows into the single `category` text field. `seeds_organizations.ts` read `number_of_employees`/`number_of_subsidiaries` (snake_case) when the actual (intentionally camelCase, see Architecture Notes) CSV headers are `numberOfEmployees`/`numberOfSubsidiaries`. Added a `buildNameLookup()` helper (`seed-utils.ts`) and used it in `seeds_universities.ts` (`universities.csv`'s `country_id`/`city_id` hold human names like `"USA"`/`"Cambridge,Massachusetts"`, not `iso_code`/UUID — resolved via a country-name-alias map + city name+state lookup after countries/cities are seeded) and in `seeds_certifications.ts`/`seeds_subjects.ts` (`issuing_organization_id`/`organization_id` hold org names). Unresolved references are logged and set to `null` (or the row is skipped where the FK is required, e.g. new `seeds_military_capabilities.ts`) instead of crashing the whole `createMany` batch. Added `seeds_military_capabilities.ts` (no seed existed for that model at all) and wired it into `seed.ts`. Also corrected stale `UserRole` values (`USER`/`MODERATOR`/`SUPER_ADMIN`, removed from the enum) still referenced in `src/models/utils/enums.ts`, `src/db/types.ts`, and `src/tests/users.test.ts`, and deleted `apps/api/prisma/test_seed_degrees.ts` (exact duplicate of `seeds/seeds_degrees.ts`, unreferenced). `apps/api` now passes `npx tsc --noEmit` and `npx prisma generate`/`migrate diff` cleanly. Correction to the entry below: re-checking with the real CSV parser (not a naive `;`-split) showed `number_of_universities` is actually clean in both `cities.csv` and `countries.csv` — only `number_of_airports` on `cities.csv` was genuinely still messy. **Not done**: no `prisma migrate dev` was run against a real database (the schema changed enough — e.g. how `City/University/...countryId` are typed — that a fresh migration is needed; this touches a live DB so it wasn't run without explicit go-ahead), and the seed hasn't been executed end-to-end against a live DB to confirm the name-based lookups actually resolve real rows (current `cities.csv` only covers cities starting with "A"/digits, so most `universities.csv` city references will resolve to `null` until more cities are added).
- [2026-07-08] Cleaned `median_salary`, `median_home_price`, `average_rent` in `apps/api/data/excel_templates/cities.xlsx` and `countries.xlsx`: stripped currency symbols, `~` approximations, thousands separators, `/month` suffixes, `Million` text, and parenthetical secondary-currency figures, leaving plain integers. The `currency` column is now filled with the ISO code matching the symbol actually used in those figures (per user decision) — this does **not** always match a country's real official currency (e.g. Turkey's salary/rent figures were quoted in €, so `currency=EUR` there even though Turkey's real currency is TRY; same pattern for Switzerland, Serbia, Hungary, etc. — ~67/100 countries.xlsx rows have this discrepancy in the source data). Cities with no currency symbol at all (~27 rows, mostly European) defaulted to `EUR`. Resaving via openpyxl drops the `currency` column's dropdown data validation (pre-existing openpyxl limitation with the extended list validation `generate_excel_templates.py` produces for the large `Currency` enum — other dropdowns like `government_type`/`cost_of_living` are unaffected). Also cleaned `number_of_international_students`, `number_of_foreign_organizations`, `number_of_universities` in `countries.xlsx` (Int fields with `~`, `+`, `<`, and comma-separated text) — stripped to plain integers; one non-numeric value (`American Samoa` / `number_of_foreign_organizations` = "Very few") became `null` since the field is nullable and no real number was recoverable. Still messy pending cleanup: `population` (String field in both files, so not type-invalid, but still has embedded "Foreign Born: X" sub-values and "X million" text) and `number_of_airports`/`number_of_universities` on `cities.xlsx` (also Int-typed there, same `+`/parenthetical patterns as countries.xlsx but not yet cleaned).
- [2026-07-07] Added `apps/api/scripts/generate_excel_templates.py`: parses `schema.prisma`, excludes relation fields and auto-managed columns (id/created_at/updated_at/deleted_at), adds enum dropdowns and type hints, outputs one `.xlsx` per model to `apps/api/data/excel_templates/`.
- [2026-07-07] Added `apps/api/scripts/excel_to_csv.py`: converts every `.xlsx` in `apps/api/data/excel/` to a `.csv` in `apps/api/data/` (active sheet only). Paths are computed from `__file__`, not hardcoded, so it works on any machine.
